Small personnal project with the purpose to learn golang.
Comand line interface Todo app.

Here are examples of commands:

go run main.go list

go run main.go add -title "item title" -c "item category"

